<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        tools:src="@mipmap/ic_launcher_round" />

    <TextView
        android:id="@+id/title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="@dimen/horizontal_margin_items"
        android:layout_marginTop="@dimen/margin_top_items"
        android:layout_marginEnd="@dimen/margin_end_titles"
        android:layout_marginBottom="@dimen/horizontal_margin_items"
        android:textColor="@color/text_color"
        android:textSize="@dimen/title_text_size"
        android:textStyle="bold"
        tools:text="@string/app_name" />

</LinearLayout>